# RECON — Nomadomics v2 (2026-09-07)

> Executor recon for the review/patch/forward goal-prompt. Every value below from a
> live command this session. Companion to `docs/PUBLISH-RUNBOOK-2026-08-30.md`.

## Live system (verified 2026-09-07)

| Check | Command | Result |
|---|---|---|
| Strapi service | `launchctl list \| grep nomadomics` | strapi PID 1377 (0) · cloudflared PID 1545 (**exit 1** — needs health check) |
| Strapi admin | `curl /admin` | **200** |
| Gateway | `hermes cron status` | **RUNNING** (PID 5968) — crons WILL fire if armed |
| Articles | API status counts | **12 published / 8 in_review** (20 total) |
| Topics | API status counts | **17 pending / 8 in_review** (25 total) |
| Engine tests | `pytest tests/ -q` | **48 passed** (9.07s) |
| Engine flags | `engine.cli info` | gemini-2.5-flash ×3 stages, provider=gemini, `premium_enabled=false`, `auto_publish_enabled=false`, threshold 80, `gemini_key_set=true` |
| Remote auth | `git ls-remote --heads origin` | OK — `main` @ f992057, `feat/content-engine-mvp` @ 68aadd2 |
| Unpushed | `git log origin/main..HEAD` | **3 commits**: 943fff2 (engine Gemini+editor+structure), 10296ef (frontend GFM+monetization guard), 098f731 (state log) |
| Divergence | `--left-right --cherry-pick` | none — clean fast-forward push |
| Dirty tree | `git status --short` | `M engine/state/pipeline.jsonl` + `?? docs/PUBLISH-RUNBOOK-2026-08-30.md` |
| PAT scan (content) | `git grep ghp_/github_pat_` over all commits | **clean** |
| PAT scan (messages) | unpushed commit subjects | **clean** |

## Doc-drift findings

| # | Finding | Where | Truth | Fix | Class |
|---|---|---|---|---|---|
| D1 | "Strapi blocked on create-strapi-app headless login" | ROADMAP §Mission C Blockers | Strapi UP since 2026-07-14, launchd, /admin=200 today | Rewrite Mission C | autonomous |
| D2 | "GitHub push: PAT repo-scope gotcha (403)" | ROADMAP §Mission C Blockers | HTTPS remote auth works (ls-remote OK); 3 commits just unpushed | Rewrite Mission C | autonomous |
| D3 | Checkboxes frozen at C1.1 (2026-07-26); no Gemini/ship-blog/frontend mention | ROADMAP §Mission C | Pipeline now Gemini 3-stage; frontend deployed on Vercel; 20 articles live | Rewrite Mission C + check off C1.x | autonomous |
| D4 | RUNBOOK predates Gemini pipeline + `AUTO_PUBLISH_ENABLED` kill-switch | `RUNBOOK.md` (last touched 08-25) | Provider chain, editor stage, structure gate, flag semantics undocumented | Add "Gemini content pipeline" section | autonomous |
| D5 | No issue_log entry for 2026-08-25→09-06 work | `~/.hermes/issue_log.md` | 3 sessions of work unlogged | Append dated entry | autonomous |
| D6 | `engine/state/*.jsonl` not gitignored → perpetual dirty tree | `.gitignore` / git | Append-only runtime event log, not source | gitignore + `git rm --cached` | autonomous |
| D7 | `docs/PUBLISH-RUNBOOK-2026-08-30.md` untracked | git | Staging doc for G1–G6 gates exists only locally | `git add` + commit | autonomous |
| D8 | Cloudflared LaunchAgent shows last-exit-status 1 in `launchctl list` | launchctl | Cosmetic: the process is alive (PID 1545, config-run) and the tunnel serves fine — admin 200 / API 401 through `strapi.nomadomics.blog` verified this session. The stale "1" is from an earlier restart; `KeepAlive+RunAtLoad` are set. No action. | none (monitor) | autonomous (checked) |

## Corrections to the goal-prompt's own baselines

- "Gateway **not running** (jobs won't fire)" → **false today**: gateway IS running (PID 5968). Raises the stakes on not arming crons accidentally — none are armed.
- Strapi counts confirmed exactly (12/8 articles, 17/8 topics).

## J1 pre-check (push readiness)

Auth verified, history PAT-clean, divergence clean → **push is a safe fast-forward**.
Handoff command for Guy: `git -C ~/nomadomics-v2 push origin main`.
