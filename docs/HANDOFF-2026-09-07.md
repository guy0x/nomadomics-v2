# HANDOFF — Nomadomics v2 (2026-09-07, end of launch-ready cycle)

> Where the project stands, what shipped, and exactly what's left for Guy.

## Shipped this cycle (2026-09-07)

### Engine
- Gemini 3-stage pipeline (research → draft → edit) live; OpenRouter `:free` fallback chain
- Editor stage (voice/structure/grounding QA) between draft and SEO scoring
- Structure contract in prompts + `structure_score` in the deterministic SEO scorer
- `AUTO_PUBLISH_ENABLED` kill-switch (default **false** — every draft lands `in_review`)
- State-log test-isolation fixed (63 mock entries archived to `pipeline.mock-archive.jsonl`)
- 48/48 tests; verified live end-to-end: `budget-travel-accommodation-hacks` (conf 92 → `in_review`)

### Content
- 13 published articles, all complete: excerpt, focusKeyword, targetKeywords, metaTitle ≤60,
  metaDescription ≤155 — written in voice, length-verified before write
- 26 branded images (13 OG 1200×630 + 13 cards 1200×675) generated via Guy's authenticated
  Gemini web session (CDP-driven), finalized with sips center-crop; served from repo
  `/og/<slug>.png` + `/cards/<slug>.png` — no Strapi schema change
- Top-3 published per `docs/PUBLISH-RUNBOOK-2026-08-30.md`; 2 colliding WP seeds retired to draft
- ⚠️ `publishedAt` stagger NOT done: Strapi v5 re-stamps it on every content-API write;
  custom backdating needs a one-time admin-JWT pass (Guy's call — or accept real dates)

### Frontend (live on Vercel)
- Homepage redesigned (Editorial money-frame): typography hero, honest stat strip,
  "Start here" flagship (pinned by slug — fixes fake "Editor's pick"), latest feed with
  card covers, honest newsletter close
- GFM markdown renderer (react-markdown + remark-gfm): headings/tables/bullets render properly
- Monetization guard: `isMonetized()` suppresses any CTA whose href isn't a real
  affiliate/referral link (Wise + Revolut point at Guy's referral links; NordVPN/Surfshark/
  SafetyWing CTAs suppressed until programs are approved)
- og:image + twitter:image wired per slug; homepage meta title "Nomadomics - Money Tips for Travelers"

### Infra / docs
- All commits pushed through `eb42225`; repo clean; PAT-scan clean
- `RUNBOOK.md`: Gemini pipeline + staged-cron sections; ROADMAP Mission C rewritten truthful
- Cron **armed**: `nomadomics-draft-batch` `0 9 * * 0-5` → `run-batch 2` → Telegram (first run 2026-09-08 09:00)
- Recon + verify docs: `docs/RECON-2026-09-07.md`, `docs/ENGINE-VERIFY-2026-09-07.md`,
  `frontend/docs/LAUNCH-READY-2026-09-07.md`

## What Guy does next (in order)

1. **Cloudflare DNS for `nomadomics.blog`** (only Guy): apex `A 76.76.21.21`, `www`
   `CNAME cname.vercel-dns.com`, both DNS-only/grey-cloud. This makes the domain live AND
   all `og:image` URLs resolve (they currently 404 on the apex until DNS lands).
2. **Review the 6 `in_review` drafts** in Strapi admin (`hi@govadia.com` / localhost:1337):
   publish good ones manually, or say the word to flip `AUTO_PUBLISH_ENABLED=true`
   (conf ≥80 non-sensitive auto-publishes; sensitive still quarantines).
3. **Affiliate applications** (only Guy): NordVPN → Impact, Surfshark → CJ/Awin,
   SafetyWing → PartnerStack. Until approved, their CTAs stay suppressed by design.
4. **Hetzner migration** when ready: `docs/DEPLOY-HETZNER.md` phases 1–5.

## Known limitations (accepted, documented)

- `publishedAt` = real publish time (not backdated-staggered) — Strapi-managed field
- Gemini web session lives in `/tmp/chrome-paydash4` (dies on reboot; regenerate images → re-login)
- First-run images may show minor AI artifacts at full zoom (gibberish laptop "code")
- Vercel deploys via git push only — do NOT run `vercel --prod` from inside `frontend/`
  (double-nests the root directory → "Root Directory does not exist" error)
