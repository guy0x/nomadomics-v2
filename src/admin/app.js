// Custom global admin styles for Nomadomics 2.0
// Enhances the article edit view: makes bodyMarkdown prominent, monospace, readable, and spacious.
import './custom.css';

const config = {
  locales: [],
};

const bootstrap = (app) => {
  // Nomadomics body-first admin customisation loaded
};

export default {
  config,
  bootstrap,
};
